// Intro component removed — no-op stub to keep imports/builds safe.
// The app no longer renders the intro overlay. Remove this file entirely later if you want a clean deletion.
export const IntroRemoved = true;
